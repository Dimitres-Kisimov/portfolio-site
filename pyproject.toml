[project]
name = "portfolio-site"
version = "1.0.0"
description = "Original, copyright-free static portfolio site generated from projects.json (stdlib only)."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
